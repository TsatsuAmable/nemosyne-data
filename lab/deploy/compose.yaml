services:
  lab:
    build:
      context: ../..
      dockerfile: lab/deploy/Dockerfile
    environment:
      NEMOSYNE_LAB_PROFILE: ${NEMOSYNE_LAB_PROFILE:-full}
    volumes:
      - lab-evidence:/evidence
volumes:
  lab-evidence: {}
