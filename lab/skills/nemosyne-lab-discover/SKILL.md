# Nemosyne Lab Discover
Use `lab.describe` first, then `capabilities.list`. Treat returned capability/protocol metadata as authoritative over model memory. Inspect `claims.status`, `runs.status`, `evidence.query`, and `findings.list` before proposing new work. Never infer physical or human qualification from simulator evidence. Never invent unavailable workers.
